# Commands reference

> Auto-generated by `scripts/build-catalog.mjs`. 124 commands.

## /ai (2)

- **`/ai:mcp-scaffold`** `<name>` — Scaffold a new MCP server (tools/resources/transport).
- **`/ai:prompt-test`** `[prompt-file]` — Evaluate an LLM prompt against cases and report results.

## /api (5)

- **`/api:contract`** `[consumer] [provider]` — Set up or check consumer-driven contract tests between services.
- **`/api:design`** `<resource-or-feature>` — Design a clean, consistent, evolvable API for a resource.
- **`/api:docs`** `[spec-or-path]` — Generate or update API documentation from the spec/code.
- **`/api:mock`** `[spec-file]` — Create a mock server/stubs from an API spec for development.
- **`/api:test-endpoint`** `<method> <url> [body]` — Exercise an API endpoint and validate its response.

## /aws (6)

- **`/aws:cost`**  — Summarize AWS cost drivers and savings (read-only).
- **`/aws:ec2`** `[--region r]` — List EC2 instances with state, type and cost signals.
- **`/aws:iam-audit`** `[user-or-role]` — Audit IAM for over-broad permissions (read-only).
- **`/aws:logs`** `<log-group>` — Tail and analyze a CloudWatch log group.
- **`/aws:s3`** `[bucket]` — List/inspect S3 buckets and flag public exposure (read-only).
- **`/aws:whoami`**  — Show the current AWS identity, account and region.

## /azure (3)

- **`/azure:aks`** `[cluster]` — Show AKS cluster status and node health.
- **`/azure:resources`** `[resource-group]` — List Azure resource groups and key resources (read-only).
- **`/azure:whoami`**  — Show the current Azure account and subscription.

## /ci (3)

- **`/ci:generate`** `[github|gitlab]` — Generate a CI workflow tailored to the detected stack.
- **`/ci:lint`** `[file]` — Validate a CI configuration file for correctness and safety.
- **`/ci:status`**  — Summarize recent CI run results and failures.

## /cloud (3)

- **`/cloud:cost`** `[aws|azure|gcp]` — Identify likely cloud cost drivers and savings.
- **`/cloud:deploy`** `[target]` — Review a deployment plan for safety before applying (no auto-apply).
- **`/cloud:inspect`** `[aws|azure|gcp]` — Summarize cloud account resources for the detected provider (read-only).

## /db (8)

- **`/db:backup`** `[db]` — Back up a database safely and verify the dump.
- **`/db:explain`** `<sql>` — Explain what a SQL query does and its performance characteristics.
- **`/db:inspect`** `[connection-or-dsn]` — Connect (read-only) and describe a database's schema, tables and sizes.
- **`/db:migrate`** `[migration-file]` — Plan or review a schema migration for safety and reversibility.
- **`/db:optimize`** `<sql>` — Diagnose a slow query using its execution plan and suggest fixes.
- **`/db:query`** `<sql>` — Run a read-only query and format the results (writes require confirmation).
- **`/db:schema`** `[table]` — Show and explain the schema of a table or the whole database.
- **`/db:seed`** `[table]` — Generate or run seed/fixture data for a schema (non-production).

## /dev (24)

- **`/dev:analyze`** `[path]` — Scan a project: language, framework, package manager, structure, risks.
- **`/dev:benchmark`** `<target>` — Measure performance of a code path and report results.
- **`/dev:changelog`** `[since-tag]` — Generate or update a changelog from commit history.
- **`/dev:comment`** `<file> [symbol]` — Add or improve code comments (explain why, not what).
- **`/dev:complexity`** `[path]` — Report the most complex / highest-churn files to target for refactor.
- **`/dev:debug`** `--file <f> --line <n> --error <msg>` — Diagnose a specific error or failure to root cause.
- **`/dev:deps-graph`** `[path]` — Map the internal module dependency graph and flag cycles.
- **`/dev:detect-bugs`** `[path]` — Scan code for likely bugs, silent failures and risky patterns.
- **`/dev:document`** `<file-or-module>` — Write or update accurate documentation for code.
- **`/dev:explain`** `<file> [symbol]` — Explain how a file, function or module works.
- **`/dev:find-deps`** `[path]` — Map and audit the project's dependencies.
- **`/dev:find-unused`** `[path]` — Find dead code, unused deps and duplication.
- **`/dev:fix`** `<bug description>` — Fix a described bug end-to-end with a regression test.
- **`/dev:format`** `[path]` — Format code with the project's configured formatter.
- **`/dev:generate`** `<what to build>` — Generate new code that matches the project's existing conventions.
- **`/dev:migrate`** `<from> -> <to>` — Plan and assist a code/framework/version migration.
- **`/dev:optimize`** `<target>` — Find and fix a real performance bottleneck by measurement.
- **`/dev:refactor`** `<file-or-area>` — Improve code structure without changing behavior, under test cover.
- **`/dev:release`** `<major|minor|patch>` — Prepare a release: bump version, changelog, tag (with confirmation).
- **`/dev:review`** `[path-or-diff]` — Review code for correctness, security, performance and clarity.
- **`/dev:scaffold`** `<type> <name>` — Bootstrap a new module/component with the project's conventions.
- **`/dev:test`** `[path]` — Detect the test framework and run tests for a path.
- **`/dev:todos`** `[path]` — Scan the codebase for TODO/FIXME/HACK and triage them.
- **`/dev:translate`** `<file> --to <lang>` — Translate code from one language/framework to another idiomatically.

## /docker (6)

- **`/docker:build`** `[path] [tag]` — Build an image and review the Dockerfile for size/security.
- **`/docker:cleanup`**  — Report reclaimable Docker space and prune safely (with confirmation).
- **`/docker:compose`** `[up|down|logs]` — Bring up / inspect a Docker Compose stack and verify health.
- **`/docker:inspect`** `<image-or-container>` — Inspect images/containers: config, ports, mounts, health.
- **`/docker:logs`** `<container>` — Fetch and analyze container logs for errors.
- **`/docker:troubleshoot`** `<container-or-image>` — Diagnose a failing/unhealthy container or build.

## /embedded (2)

- **`/embedded:flash`** `[port]` — Build and flash firmware to a connected device (with confirmation).
- **`/embedded:monitor`** `<port> [baud]` — Open a serial monitor and analyze device output.

## /files (7)

- **`/files:analyze`** `<file>` — Detect a file's real type, encoding, size and metadata.
- **`/files:bulk-rename`** `<pattern> <replacement> [path]` — Preview and apply a bulk rename by pattern (with confirmation).
- **`/files:convert`** `<file> --to <format>` — Convert a file between formats losslessly.
- **`/files:dedupe`** `[path]` — Find duplicate files by content hash (report only).
- **`/files:hash`** `<file> [expected-hash]` — Compute and/or verify a file's checksum.
- **`/files:search`** `<query> [path]` — Search file contents or names across the project.
- **`/files:tree`** `[path] [depth]` — Show an annotated project structure overview.

## /game (1)

- **`/game:profile`** `[target]` — Analyze game performance: frame time, draw calls, GC, hotspots.

## /gcp (3)

- **`/gcp:gke`** `[cluster]` — Show GKE cluster status and node health.
- **`/gcp:resources`** `[project]` — List key GCP resources for the current project (read-only).
- **`/gcp:whoami`**  — Show the active gcloud account and project.

## /git (13)

- **`/git:bisect`** `<good-ref> <bad-ref>` — Find the commit that introduced a bug via bisection.
- **`/git:blame`** `<file> [line-range]` — Find who/when/why a line or region was last changed.
- **`/git:branch`** `[name]` — Create, list or switch branches following the project's convention.
- **`/git:cleanup`**  — Identify merged branches and cruft to clean up (with confirmation).
- **`/git:commit`** `[message]` — Craft a clear commit message and commit staged changes (with confirmation).
- **`/git:diff`** `[ref-or-file] [ref]` — Show and explain a diff between refs or files.
- **`/git:log`** `[path-or-author-or-range]` — Summarize commit history, optionally filtered.
- **`/git:pr-prep`** `[base]` — Prepare a branch for a pull request: summary, checklist, diff review.
- **`/git:review`** `[--staged]` — Review staged/unstaged changes before committing.
- **`/git:stash`** `[save|pop|list] [message]` — Safely stash or restore work in progress.
- **`/git:status`**  — Summarize working tree, branch and recent history clearly.
- **`/git:sync`** `[--rebase]` — Fetch, review incoming changes, and update the branch safely.
- **`/git:undo`** `[soft|mixed|last-commit]` — Safely undo the last commit/change without losing work.

## /k8s (5)

- **`/k8s:describe`** `<kind/name> [namespace]` — Describe a resource and explain its events/state.
- **`/k8s:logs`** `<pod> [namespace]` — Fetch and analyze logs for a pod (including previous crash).
- **`/k8s:pods`** `[namespace]` — List pods and highlight unhealthy ones.
- **`/k8s:rollout`** `<deployment> [namespace]` — Check a deployment's rollout status and history.
- **`/k8s:top`** `[pods|nodes]` — Show pod/node resource usage to find hotspots.

## /pipeline (3)

- **`/pipeline:create`** `<name> <steps>` — Define a reusable pipeline from a sequence of commands.
- **`/pipeline:run`** `<steps-or-name>` — Run a named or ad-hoc multi-step pipeline (analyze -> test -> build).
- **`/pipeline:status`**  — Show the status/logs of the most recent pipeline run.

## /project (4)

- **`/project:detect`** `[path]` — Detect language, framework, package manager and build system.
- **`/project:healthcheck`** `[path]` — Assess project health: tests, lint, deps, docs, CI.
- **`/project:onboard`** `[path]` — Produce a newcomer onboarding overview of the project.
- **`/project:structure`** `[path]` — Explain the project's architecture and module boundaries.

## /security (6)

- **`/security:audit`** `[path]` — Run a defensive security audit of the codebase.
- **`/security:deps`** `[path]` — Audit dependencies for known vulnerabilities.
- **`/security:headers`** `<url>` — Check a URL's HTTP security headers and suggest hardening.
- **`/security:permissions`** `[path-or-policy]` — Review file/dir or IAM permissions for least-privilege issues.
- **`/security:secrets`** `[path]` — Scan the repo and history for leaked credentials.
- **`/security:threat-model`** `<feature>` — Produce a lightweight STRIDE threat model for a feature.

## /system (5)

- **`/system:disk`** `[path]` — Report disk usage and find large files/directories.
- **`/system:env`** `[filter]` — Show relevant environment variables (secrets redacted).
- **`/system:inspect`**  — Summarize OS, CPU, memory, disk and key runtimes.
- **`/system:network`** `[host-or-port]` — Diagnose local connectivity, DNS and open ports.
- **`/system:processes`** `[cpu|mem]` — Show top resource-consuming processes.

## /test (6)

- **`/test:coverage`** `[path]` — Measure test coverage and highlight meaningful gaps.
- **`/test:e2e`** `[journey]` — Create or run end-to-end tests for a user journey.
- **`/test:generate`** `<file> [symbol]` — Generate meaningful tests for a file or function.
- **`/test:run`** `[path-or-pattern]` — Detect the framework and run the test suite.
- **`/test:tdd`** `<feature>` — Drive a feature with red-green-refactor TDD.
- **`/test:watch`** `[path]` — Run tests in watch mode for a fast feedback loop.

## /tf (2)

- **`/tf:plan`** `[dir]` — Run terraform plan and review the change for safety.
- **`/tf:validate`** `[dir]` — Validate and format Terraform configuration.

## /udt (4)

- **`/udt:config`** `[key]` — Show or explain UDT configuration (udt.config.yaml) and how to change it.
- **`/udt:doctor`** `[--json]` — Detect installed tools/runtimes and report which UDT skills are actually usable.
- **`/udt:help`** `[command-or-skill]` — List UDT commands and skills, or explain a specific one.
- **`/udt:registry`** `[keyword]` — Search and list all available UDT commands by keyword.

## /web3 (3)

- **`/web3:audit`** `<file>` — Defensive review of a smart contract for common vulnerabilities.
- **`/web3:compile`** `[dir]` — Compile smart contracts and report size/warnings.
- **`/web3:test`** `[pattern]` — Run smart-contract tests (unit/fuzz/invariant) with gas report.

