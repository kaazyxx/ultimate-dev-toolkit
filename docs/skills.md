# Skills reference

> Auto-generated by `scripts/build-catalog.mjs`. 384 skills.

## 3d Graphics (2)

- **`blender-mcp`** — Drive Blender from Claude through the Blender MCP server (BlenderMCP): create and edit scenes/objects, assign materials and textures, import PolyHaven/Hyper3D assets, run Python in Blender, and render. Use when controlling Blender programmatically via MCP. Requires Blender plus the BlenderMCP add-on and the blender-mcp server actually installed and connected.
- **`texture-3d`** — Create and apply 3D textures and PBR materials: albedo/base-color, normal, roughness, metallic, ambient-occlusion, height/displacement and emission maps, UV unwrapping, texture baking and texel density. Use for 3D texturing, material authoring, and texture sets (.png/.exr/.hdr). Detect Blender/ImageMagick before claiming you can bake or process maps.

## Ai (7)

- **`ai-agent-orchestration`** — Orchestrate multi-step/multi-agent AI systems: planning, tool routing, memory, budgets, evaluation, guardrails. Use for agentic pipelines.
- **`function-calling`** — Design LLM tool/function-calling: JSON schemas, validation, error handling, parallel calls, determinism. Use when wiring tools to an LLM.
- **`image-generation`** — Use image-generation models/APIs (diffusion): prompts, control (ControlNet/img2img), safety, cost, upscaling. Use for programmatic image gen.
- **`mcp-server-dev`** — Build MCP (Model Context Protocol) servers: tools, resources, prompts, transports (stdio/SSE), schemas. Use when creating an MCP server for Claude/agents.
- **`ocr-extraction`** — Extract text/structure from images and PDFs (Tesseract/vision models): preprocessing, layout, tables, confidence. Use for document/OCR pipelines.
- **`speech-processing`** — Build speech features: speech-to-text (Whisper), text-to-speech, diarization, streaming, latency. Use for voice/audio AI.
- **`vector-databases`** — Operate vector databases (Pinecone/Weaviate/Qdrant/pgvector/Milvus): indexes (HNSW/IVF), metadata filters, hybrid search, cost. Use for semantic search/RAG storage.

## Architecture (18)

- **`algorithms-data-structures`** — Choose the right algorithm/data structure and reason about complexity (Big-O), tradeoffs, and correctness. Use for algorithmic problems.
- **`api-design`** — Design clear, consistent, evolvable APIs (REST/GraphQL/RPC): naming, versioning, errors, pagination, backward compatibility. Use when designing any API.
- **`architecture-patterns`** — Choose and apply architectural patterns (layered, hexagonal, microservices, monolith, event-driven) for the real constraints. Use when structuring a system.
- **`clean-architecture`** — Apply clean/onion architecture: dependency inversion, domain independence from frameworks, testable boundaries. Use when isolating business logic.
- **`concurrency-patterns`** — Write correct concurrent code: threads, async, locks, channels, immutability, avoiding races/deadlocks. Use for multithreaded/async code.
- **`cqrs-event-sourcing`** — Apply CQRS and event sourcing where justified: separate read/write models, event log as source of truth. Use for audit-heavy/complex-write domains.
- **`design-patterns`** — Apply classic design patterns (factory, strategy, observer, adapter, etc.) where they simplify, not where they add ceremony. Use when structuring code.
- **`distributed-systems`** — Reason about distributed systems: CAP, consistency models, consensus, partial failure, idempotency, clocks. Use for multi-node systems.
- **`domain-driven-design`** — Apply DDD: ubiquitous language, bounded contexts, aggregates, entities/value objects. Use for complex business domains.
- **`event-driven-architecture`** — Design event-driven systems: events, pub/sub, choreography vs orchestration, eventual consistency. Use for decoupled/async systems.
- **`functional-programming`** — Apply functional techniques: pure functions, immutability, composition, higher-order functions, effects at edges. Use to reduce state bugs.
- **`hexagonal-architecture`** — Apply ports-and-adapters (hexagonal) architecture: core logic behind ports, adapters for IO. Use to isolate and test core logic.
- **`idempotency-patterns`** — Make operations idempotent: idempotency keys, dedup, upserts, exactly-once semantics. Use for retryable/async operations and payments.
- **`memory-management`** — Manage memory correctly: allocation, ownership, leaks, GC tuning, fragmentation across languages. Use for memory bugs/leaks/tuning.
- **`oop-principles`** — Apply OOP/SOLID sensibly: encapsulation, composition over inheritance, single responsibility, dependency inversion. Use when designing OO code.
- **`scalability-patterns`** — Scale systems: horizontal scaling, sharding, replication, caching, async, statelessness, back-pressure. Use when a system must handle more load.
- **`system-design`** — Design systems at scale: requirements, capacity, data flow, storage, caching, tradeoffs, failure modes. Use for high-level design tasks/interviews.
- **`twelve-factor-app`** — Apply the twelve-factor methodology: config in env, stateless processes, logs as streams, dev/prod parity. Use for cloud-native apps.

## Automation (9)

- **`automation-scripting`** — Automate repetitive tasks with scripts (shell/Python/PowerShell): idempotency, error handling, logging, dry-run. Use to eliminate manual toil.
- **`browser-automation`** — Automate browsers (Playwright/Puppeteer/Selenium): navigation, forms, waits, headless, resilience. Use for UI automation of permitted sites.
- **`csv-processing`** — Process CSV/TSV robustly: delimiters, quoting, encoding, large files, edge cases. Use for tabular text data.
- **`data-format-conversion`** — Convert between data formats (JSON/YAML/TOML/XML/CSV/Parquet) losslessly with the right tools. Use for format conversion/interop.
- **`file-format-handling`** — Read/write/validate structured file formats (JSON/YAML/TOML/XML/INI): schema validation, safe parsing. Use when handling config/data files.
- **`image-processing`** — Process images (Pillow/ImageMagick/sharp): resize, convert, optimize, metadata, batch. Use for image manipulation.
- **`pdf-processing`** — Extract, generate and manipulate PDFs (pdfplumber/pypdf/pdftotext): text/tables, merging, forms. Use for PDF tasks.
- **`text-processing`** — Process text at scale (grep/sed/awk/jq/Python): parsing, extraction, transformation, encoding. Use for text/log/data wrangling.
- **`web-scraping-legal`** — Scrape web data responsibly and legally: robots.txt, terms of service, rate limiting, public data only, no auth/paywall bypass. Use for permitted data collection.

## Core (5)

- **`command-registry`** — The single source of truth for every UDT command: how they are named, discovered, documented and searched. Use to list, search or explain available commands, or when adding a new one.
- **`environment-detection`** — Detect the real OS, CPU, runtimes, compilers, package managers, containers, databases and project type before doing anything. Use at the start of any task so capabilities reflect what is actually installed.
- **`file-operations`** — Safely read, search, compare, move, copy, rename, convert, compress, hash and inspect files and their metadata, with confirmation before destructive changes. Use for filesystem tasks.
- **`pipelines`** — Chain commands into an ordered pipeline (e.g. analyze then test then fix then format then build) with dependencies, error handling, retries, timeouts, conditional and parallel steps. Use for multi-step automation.
- **`safe-execution`** — The security contract every UDT action passes through: validate arguments, block path traversal, avoid shell injection, redact secrets, require confirmation for destructive operations, and keep an audit trail. Use whenever a command runs a shell process or touches the filesystem.

## Data Ml (35)

- **`ai-api-integration`** — Integrate hosted AI APIs (Claude, OpenAI, etc.): auth, streaming, retries, cost, rate limits, caching. Use when calling AI provider APIs.
- **`computer-vision`** — Build computer vision models: augmentation, transfer learning, detection/segmentation, evaluation. Use for image tasks.
- **`data-airflow`** — Orchestrate data workflows with Apache Airflow/Prefect/Dagster: DAGs, scheduling, retries, backfills. Use for pipeline orchestration.
- **`data-cleaning`** — Clean and validate messy data: types, duplicates, missing values, encodings, schema validation. Use when preparing raw data.
- **`data-dbt`** — Transform warehouse data with dbt: models, tests, sources, incremental materializations. Use for dbt projects.
- **`data-etl-pipelines`** — Build ETL/ELT pipelines: extract, transform, load, idempotency, incremental processing. Use when moving/transforming data between systems.
- **`data-feature-engineering`** — Engineer features for ML: encoding, scaling, aggregation, leakage-free transforms, feature stores. Use when preparing model inputs.
- **`data-numpy`** — Compute with NumPy arrays: vectorization, broadcasting, dtypes, memory layout. Use for numerical array work in Python.
- **`data-pandas`** — Manipulate tabular data with pandas: loading, cleaning, joins, groupby, reshaping. Use for pandas/.csv/.parquet analysis in Python.
- **`data-polars`** — Use Polars for fast, memory-efficient dataframes with lazy execution. Use for large tabular data in Python/Rust.
- **`data-science-eda`** — Perform exploratory data analysis: distributions, missingness, correlations, outliers, leakage checks. Use before modeling.
- **`data-spark`** — Process big data with Apache Spark (PySpark/Scala): DataFrames, partitions, shuffles, joins. Use for large-scale distributed data. Detect spark first.
- **`data-streaming`** — Build streaming data pipelines (Kafka Streams/Flink/Spark Streaming): windowing, exactly-once, state. Use for realtime data processing.
- **`data-visualization`** — Visualize data effectively (matplotlib/seaborn/plotly): right chart, honest axes, accessible color. Use when charting data.
- **`data-warehousing`** — Model analytical warehouses (Snowflake/BigQuery/Redshift): star schema, partitioning, clustering, cost. Use for warehouse design/queries.
- **`jupyter-notebooks`** — Use Jupyter notebooks well: reproducible order, cleared outputs in VCS, kernel hygiene, promotion to scripts. Use for .ipynb work.
- **`llm-agents`** — Build LLM agents/tool-use systems: tool definitions, loops, guardrails, budgets, evaluation. Use when building agentic workflows.
- **`llm-embeddings`** — Use text/vector embeddings: models, similarity, vector databases, indexing, hybrid search. Use for semantic search/RAG.
- **`llm-evaluation`** — Evaluate LLM outputs: golden sets, LLM-as-judge, rubrics, regression tests, hallucination checks. Use to measure LLM quality.
- **`llm-fine-tuning`** — Fine-tune or adapt LLMs (LoRA/PEFT): data prep, training, evaluation, when NOT to fine-tune. Use when adapting models.
- **`llm-prompting`** — Engineer effective LLM prompts: clear instructions, examples, structure, output constraints, evaluation. Use when building LLM features.
- **`llm-rag`** — Build retrieval-augmented generation: chunking, embeddings, vector search, reranking, grounding. Use for RAG systems.
- **`ml-evaluation`** — Evaluate models rigorously: right metrics, baselines, confidence, error analysis, fairness. Use when judging model quality.
- **`ml-experiment-tracking`** — Track ML experiments (MLflow/W&B): params, metrics, artifacts, comparisons, reproducibility. Use when running many experiments.
- **`ml-hyperparameter-tuning`** — Tune hyperparameters efficiently: grid/random/Bayesian search, cross-validation, avoiding overfit-to-val. Use when optimizing models.
- **`ml-mlops`** — Operationalize ML: data/model versioning, CI for models, reproducibility, monitoring, retraining. Use for production ML systems.
- **`ml-model-serving`** — Serve ML models in production: batch vs online, latency, versioning, monitoring, rollback. Use when deploying models.
- **`ml-pytorch`** — Build and train neural networks with PyTorch: tensors, autograd, modules, training loops, GPU. Use for PyTorch models. Detect torch first.
- **`ml-scikit-learn`** — Build classical ML with scikit-learn: pipelines, cross-validation, metrics, model selection. Use for tabular ML in Python.
- **`ml-tensorflow`** — Build and train models with TensorFlow/Keras: layers, tf.data, training, SavedModel. Use for TF/Keras models. Detect tensorflow first.
- **`ml-training-pipeline`** — Design reproducible training pipelines: data splits, seeds, checkpoints, early stopping, logging. Use when training models seriously.
- **`nlp-fundamentals`** — Process natural language: tokenization, embeddings, classification, NER, evaluation. Use for text/NLP tasks.
- **`recommender-systems`** — Build recommenders: collaborative/content filtering, ranking, cold start, offline/online eval. Use for recommendation features.
- **`statistics-fundamentals`** — Apply core statistics: sampling, hypothesis testing, confidence intervals, p-values, common fallacies. Use when reasoning about data/experiments.
- **`timeseries-forecasting`** — Forecast time series: stationarity, seasonality, backtesting, classical vs ML models. Use for forecasting tasks.

## Databases (23)

- **`db-backup-restore`** — Plan and perform database backups and restores; verify recoverability. Use for backup/restore and DR tasks.
- **`db-cassandra`** — Model and query Cassandra/ScyllaDB: partition keys, denormalization, CQL. Use for CQL and Cassandra connections. Detect cqlsh first.
- **`db-clickhouse`** — Use ClickHouse for analytical/OLAP workloads: MergeTree, columnar storage, materialized views. Use for ClickHouse. Detect clickhouse-client first.
- **`db-cockroachdb`** — Use CockroachDB (distributed SQL, Postgres-compatible): ranges, transactions, locality. Use for CockroachDB. Detect cockroach/psql first.
- **`db-duckdb`** — Use DuckDB for fast local analytical SQL over files (Parquet/CSV). Use for .duckdb and analytical queries on files. Detect duckdb first.
- **`db-dynamodb`** — Model DynamoDB with single-table design: partition/sort keys, GSIs, access patterns. Use for DynamoDB. Detect aws cli first.
- **`db-elasticsearch`** — Index and search with Elasticsearch/OpenSearch: mappings, analyzers, queries, aggregations. Use for ES/OpenSearch clusters. Detect connectivity first.
- **`db-indexing`** — Design effective indexes: composite, covering, partial, and when NOT to index. Use when queries are slow or over-indexed.
- **`db-influxdb`** — Store and query time-series data with InfluxDB: measurements, tags, retention. Use for time-series/metrics storage. Detect influx first.
- **`db-migrations`** — Write safe, reversible schema migrations with zero-downtime patterns (expand/contract). Use when changing schemas.
- **`db-mongodb`** — Model, query and index MongoDB: documents, aggregation pipeline, schema design. Use for MongoDB connections. Detect mongosh first.
- **`db-mysql`** — Design, query and tune MySQL/MariaDB: storage engines, indexes, EXPLAIN, charsets. Use for MySQL connections. Detect the mysql client first.
- **`db-neo4j`** — Model and query graphs with Neo4j/Cypher: nodes, relationships, traversals. Use for Cypher and Neo4j connections. Detect cypher-shell first.
- **`db-nosql-modeling`** — Model NoSQL data by access pattern rather than normalization: denormalization, embedding vs referencing. Use when designing document/key-value/wide-column schemas.
- **`db-oracle`** — Write PL/SQL and work with Oracle: packages, cursors, hints, sequences. Use for PL/SQL and Oracle connections. Detect sqlplus first.
- **`db-orm-patterns`** — Use ORMs (Prisma, SQLAlchemy, TypeORM, Hibernate, Eloquent, ActiveRecord) without N+1 and leaky abstractions. Use for ORM code.
- **`db-postgres`** — Design, query, tune and administer PostgreSQL: types, indexes, EXPLAIN, JSONB, extensions. Use for Postgres connections and .sql on Postgres. Detect psql first.
- **`db-query-optimization`** — Diagnose and fix slow queries using execution plans, indexes and rewrites. Use for slow SQL/queries.
- **`db-redis`** — Use Redis for caching, sessions, queues, pub/sub, rate limiting: data types, TTLs, persistence. Use for Redis connections. Detect redis-cli first.
- **`db-sqlite`** — Use SQLite for embedded/local storage: pragmas, WAL, types, limitations. Use for .db/.sqlite files. Detect sqlite3 first.
- **`db-sqlserver`** — Write T-SQL and work with SQL Server: stored procs, indexes, execution plans. Use for T-SQL and SQL Server connections. Detect sqlcmd first.
- **`db-transactions`** — Use transactions and isolation levels correctly: atomicity, locking, deadlocks, retries. Use for multi-step data changes.
- **`sql`** — Write and reason about portable SQL: joins, aggregation, window functions, CTEs, set operations. Use for .sql files and query questions across engines.

## Design (2)

- **`design-systems`** — Build and use design systems: tokens, components, consistency, theming, documentation. Use for scalable, consistent UI.
- **`make-interfaces-feel-better`** — Improve UI/UX quality: hierarchy, spacing, motion, feedback, empty/loading/error states, polish. Use when a UI feels rough.

## Devops (42)

- **`artifact-registry-ops`** — Manage package/artifact/image registries: versioning, retention, promotion, signing. Use for registry and release-artifact ops.
- **`caddy-traefik`** — Configure Caddy or Traefik as auto-TLS reverse proxies/ingress. Use for Caddyfile/Traefik config.
- **`capacity-planning`** — Plan capacity and autoscaling: load testing, headroom, cost/performance tradeoffs. Use when sizing infrastructure.
- **`chaos-engineering`** — Test resilience with controlled fault injection (latency, failures, resource limits). Use to validate reliability assumptions.
- **`ci-cd-github-actions`** — Build CI/CD with GitHub Actions: workflows, jobs, matrix, caching, secrets, reusable workflows. Use for .github/workflows.
- **`ci-cd-gitlab`** — Build CI/CD with GitLab CI: stages, jobs, rules, artifacts, caches, environments. Use for .gitlab-ci.yml.
- **`ci-cd-jenkins`** — Build pipelines with Jenkins declarative/scripted pipelines. Use for Jenkinsfile.
- **`ci-cd-pipeline-design`** — Design fast, reliable CI/CD pipelines regardless of provider: caching, parallelism, gates, artifacts, promotion. Use when designing/improving pipelines.
- **`cron-scheduling`** — Schedule reliable recurring jobs with cron/systemd timers/Task Scheduler: locking, logging, alerting on failure. Use for scheduled tasks.
- **`deployment-strategies`** — Deploy safely: blue-green, canary, rolling, feature flags, and fast rollback. Use when planning a release/rollout.
- **`docker-compose`** — Define and run multi-container apps with Docker Compose: services, networks, volumes, env. Use for compose.yaml/docker-compose.yml.
- **`docker-fundamentals`** — Build, run and debug Docker images and containers: Dockerfiles, layers, volumes, networks, logs. Use for Dockerfile and container issues. Detect docker first.
- **`docker-image-optimization`** — Shrink and harden container images: multi-stage builds, layer caching, distroless, scanning. Use when images are large or insecure.
- **`feature-flags`** — Use feature flags for progressive delivery, kill switches and experiments. Use when decoupling deploy from release.
- **`gitops-argocd`** — Implement GitOps with Argo CD/Flux: declarative deploys, sync, drift detection. Use for GitOps workflows.
- **`iac-ansible`** — Automate configuration with Ansible: playbooks, roles, inventories, idempotency. Use for playbook.yml/roles. Detect ansible first.
- **`iac-cloudformation`** — Provision AWS with CloudFormation/CDK/SAM templates: stacks, changesets, drift. Use for AWS templates. Detect aws cli first.
- **`iac-packer-vagrant`** — Build machine images with Packer and dev VMs with Vagrant. Use for .pkr.hcl and Vagrantfile. Detect the tools first.
- **`iac-pulumi`** — Manage infrastructure as code with Pulumi in a real programming language. Use for Pulumi projects. Detect pulumi first.
- **`iac-terraform`** — Manage infrastructure as code with Terraform/OpenTofu: providers, state, modules, plan/apply. Use for .tf files. Detect terraform/tofu first.
- **`incident-response`** — Run incident response: detect, triage, mitigate, communicate, and write blameless postmortems. Use during/after outages.
- **`kubernetes-core`** — Work with Kubernetes: pods, deployments, services, configmaps, secrets, probes, resources. Use for k8s manifests and cluster ops. Detect kubectl first.
- **`kubernetes-helm`** — Package and deploy Kubernetes apps with Helm charts: templates, values, releases. Use for Chart.yaml/helm projects. Detect helm first.
- **`kubernetes-kustomize`** — Manage Kubernetes config with Kustomize overlays: bases, patches, per-env config. Use for kustomization.yaml.
- **`kubernetes-troubleshooting`** — Diagnose Kubernetes failures: CrashLoopBackOff, ImagePullBackOff, pending pods, OOMKilled, networking. Use when pods/services misbehave.
- **`linux-administration`** — Administer Linux servers: systemd, users/permissions, packages, cron, disk/process management. Use for server ops on Linux.
- **`load-balancing`** — Design load balancing and traffic management: L4/L7, algorithms, health checks, sticky sessions. Use when distributing traffic.
- **`log-analysis`** — Analyze logs to find root cause: parsing, grep/jq/awk, timelines, correlation across services. Use when investigating incidents from logs.
- **`message-broker-ops`** — Operate Kafka/RabbitMQ/NATS: topics/queues, partitions, consumer groups, retention, DLQ. Use for broker setup/ops.
- **`microservices-patterns`** — Design microservices: boundaries, communication, resilience (circuit breaker, retry, timeout), saga. Use when splitting or integrating services.
- **`nginx-config`** — Configure and debug NGINX: reverse proxy, TLS, caching, rate limiting, load balancing. Use for nginx.conf. Detect nginx first.
- **`observability-grafana`** — Build Grafana dashboards and alerts over metrics/logs/traces. Use when visualizing observability data.
- **`observability-logging`** — Centralize and query logs (ELK/Loki): structured logging, correlation, retention. Use for log aggregation/analysis.
- **`observability-opentelemetry`** — Add distributed tracing and metrics with OpenTelemetry: spans, context propagation, exporters. Use when tracing across services.
- **`observability-prometheus`** — Instrument and query metrics with Prometheus/PromQL: exporters, scraping, recording/alerting rules. Use for metrics/monitoring.
- **`podman`** — Use Podman as a daemonless, rootless Docker alternative: pods, systemd integration. Use when podman is the container runtime. Detect podman first.
- **`reverse-proxy-tls`** — Set up TLS termination, certificates (Let's Encrypt/ACME), HSTS and modern cipher configs. Use for HTTPS/cert setup.
- **`secrets-management-ops`** — Manage secrets across environments with Vault/SOPS/cloud secret managers: rotation, least access, no plaintext. Use for secret handling in infra.
- **`service-mesh`** — Use a service mesh (Istio/Linkerd) for mTLS, traffic shaping, and observability. Use when adopting/operating a mesh.
- **`sre-slo`** — Define SLIs/SLOs/error budgets and design for reliability. Use when setting reliability targets and alerting.
- **`system-monitoring`** — Monitor host resources: CPU, memory, disk, IO, network, processes; find resource hogs. Use for performance/capacity triage.
- **`windows-admin`** — Administer Windows systems with PowerShell: services, scheduled tasks, registry, WMI/CIM, event logs. Use for Windows server/desktop ops.

## Embedded (5)

- **`arduino`** — Program Arduino/microcontrollers (C++): setup/loop, GPIO, interrupts, timers, serial, memory limits. Use for .ino/embedded C++ on AVR/ESP. Detect the toolchain first.
- **`embedded-c`** — Write firmware in C for MCUs: registers, memory-mapped IO, interrupts, DMA, no-heap patterns, real-time constraints. Use for bare-metal/RTOS C.
- **`mqtt-iot`** — Build IoT messaging with MQTT: topics, QoS, retained/LWT messages, TLS, constrained devices. Use for IoT device/broker comms.
- **`raspberry-pi`** — Build on Raspberry Pi / Linux SBCs: GPIO, I2C/SPI, camera, systemd services, headless setup. Use for Pi/SBC projects.
- **`rtos-firmware`** — Build on an RTOS (FreeRTOS/Zephyr): tasks, priorities, queues, semaphores, ISR-safe APIs, priority inversion. Use for RTOS firmware.

## Engineering (10)

- **`code-analysis`** — Understand an unfamiliar codebase: map structure, entry points, dependencies, hotspots, complexity and risky areas before changing anything. Use when onboarding to a repo or scoping a change.
- **`code-generation`** — Generate new code that matches the project's existing conventions, patterns and dependencies rather than a generic template. Use when scaffolding modules, functions, endpoints or tests.
- **`code-review-playbook`** — Review a diff for correctness, security, performance and maintainability with prioritized, actionable feedback. Use before merging changes or when asked to review code.
- **`debugging`** — Systematically diagnose a specific failure: reproduce, isolate, form and test hypotheses, and find the root cause rather than patching symptoms. Use when given an error, stack trace or misbehavior.
- **`documentation-writer`** — Write and update accurate documentation: READMEs, API docs, docstrings, ADRs and guides derived from the real code and behavior. Use when documenting a project or change.
- **`observability`** — Make a running system legible: structured logs, metrics, traces, and process/resource monitoring, plus turning raw errors into actionable diagnostics. Use when investigating production or performance issues.
- **`performance-profiling`** — Find and fix real performance bottlenecks with measurement (profiling, benchmarks) rather than guesswork, covering CPU, memory, IO and algorithmic complexity. Use when something is slow.
- **`project-scaffolding`** — Bootstrap a new project or module with a sensible, conventional structure, config, tooling and CI for the chosen stack. Use when starting something new.
- **`refactoring`** — Improve code structure without changing behavior: extract, rename, deduplicate, simplify and reduce complexity under the safety net of tests. Use when code is hard to read or change but works.
- **`testing-strategy`** — Choose and apply the right tests (unit, integration, e2e, property, snapshot) and detect the project's real test framework and coverage tooling. Use when adding tests or deciding what to test.

## Game Graphics (13)

- **`game-audio`** — Implement game audio: mixing, spatial/3D sound, DSP, latency, middleware (FMOD/Wwise). Use for game sound systems.
- **`game-godot-engine`** — Build games in Godot (GDScript/C#): nodes, scenes, signals, the game loop, physics bodies. Use for Godot projects beyond a single script.
- **`game-loop-patterns`** — Design robust game loops: fixed vs variable timestep, interpolation, decoupling update from render, determinism. Use when building/fixing a game loop.
- **`game-networking`** — Build multiplayer networking: client prediction, server reconciliation, lag compensation, snapshot interpolation, authority. Use for netcode.
- **`game-unity`** — Build games in Unity (C#): MonoBehaviour lifecycle, prefabs, physics, scenes, the update loop, profiling. Use for Unity projects and .cs game scripts. Detect Unity before claiming you can build.
- **`game-unreal`** — Build games in Unreal Engine (C++/Blueprints): actors, components, gameplay framework, ticking, replication. Use for Unreal projects and .cpp/.h gameplay code.
- **`physics-simulation`** — Simulate physics: rigid bodies, collision detection/response, integration, constraints, stability. Use for game/simulation physics work.
- **`procedural-generation`** — Generate content procedurally: noise (Perlin/Simplex), seeds, wave function collapse, L-systems, reproducibility. Use for procgen terrain/levels/textures.
- **`shader-programming`** — Write shaders across APIs (GLSL/HLSL/WGSL): lighting models, PBR, post-processing, compute. Use for shader authoring beyond WebGL. Detect the toolchain first.
- **`sprite-animation`** — Build 2D sprite animation: atlases, frame timing, state machines, skeletal (Spine/DragonBones). Use for 2D game animation.
- **`threejs`** — Build 3D web scenes with Three.js: scene/camera/renderer, geometries, materials, lights, the render loop, GLTF loading. Use for Three.js/WebGL projects.
- **`webgl-shaders`** — Write GLSL shaders for WebGL/Three.js: vertex/fragment stages, uniforms, attributes, varyings, precision. Use for custom shaders and visual effects.
- **`webxr-ar-vr`** — Build AR/VR experiences (WebXR/Unity XR): stereo rendering, controllers, comfort, performance budgets. Use for XR projects.

## Languages (59)

- **`ada`** — Write and build Ada/SPARK for safety-critical code: strong typing, contracts, tasking. Use for .adb/.ads files. Detect gnat first.
- **`apl`** — Read and write array languages (APL/J/K/BQN): array-oriented, tacit, terse operators. Use for array-language sources. Detect the interpreter (dyalog/j/ngn-k) first.
- **`assembly`** — Read, write and debug x86-64/ARM assembly: registers, calling conventions, memory, syscalls. Use for .s/.asm files and low-level debugging. Detect the assembler/objdump first.
- **`c`** — Write, compile, analyze and debug C with attention to memory safety and undefined behavior. Use for .c/.h files, Makefiles, and segfault/UB debugging.
- **`clojure`** — Write and run Clojure/ClojureScript with a REPL-driven workflow and immutable data. Use for .clj/.cljs files and deps.edn/project.clj. Detect clojure/lein first.
- **`cobol`** — Read and maintain COBOL business/legacy code: divisions, PIC clauses, records, files. Use for .cob/.cbl files. Detect cobc (GnuCOBOL) first.
- **`cpp`** — Write and review modern C++ (17/20): RAII, smart pointers, move semantics, templates, concurrency. Use for .cpp/.hpp files, CMake projects, and C++ build/UB issues.
- **`crystal`** — Write Crystal, a Ruby-like statically typed compiled language. Use for .cr files and shard.yml. Detect crystal first.
- **`csharp`** — Write, build and test C# (.NET): async/await, LINQ, nullable reference types. Use for .cs files, .csproj/.sln projects and .NET build errors.
- **`css`** — Write and debug modern CSS: layout (flexbox/grid), responsive design, custom properties, specificity. Use for .css files and styling/layout issues.
- **`d-lang`** — Write and build D language code with its compilers and dub package manager. Use for .d files and dub.json/dub.sdl. Detect dmd/ldc2 before claiming support.
- **`dart-language`** — Write, analyze and test Dart (Flutter and standalone): null safety, async, isolates. Use for .dart files and pubspec.yaml. Detect dart first.
- **`elixir`** — Write, test and build Elixir (Mix, OTP, Phoenix): processes, supervision, immutability. Use for .ex/.exs files and mix.exs. Detect elixir/mix first.
- **`erlang`** — Write and build Erlang/OTP: actors, supervision, hot code. Use for .erl files and rebar3 projects. Detect erl/rebar3 first.
- **`fish`** — Write Fish shell scripts and functions. Use for .fish files and config.fish. Detect the fish binary before executing.
- **`fortran`** — Write and compile Fortran (77/90+) for numerical/HPC code: arrays, modules, intrinsics. Use for .f90/.f files. Detect gfortran first.
- **`fsharp`** — Write and build F# (.NET): immutability, discriminated unions, pattern matching, computation expressions. Use for .fs/.fsx files.
- **`gdscript`** — Write GDScript for Godot game logic: nodes, signals, scenes, the game loop. Use for .gd files. Detect the godot binary first.
- **`go`** — Write, vet, test and build idiomatic Go: goroutines, channels, explicit error handling, interfaces. Use for .go files and go.mod projects.
- **`graphql-lang`** — Design GraphQL schemas (SDL): types, queries, mutations, subscriptions, evolution. Use for .graphql/.gql schema files.
- **`groovy`** — Read and write Groovy, including Gradle build scripts and Jenkins pipelines. Use for .groovy files and Jenkinsfile.
- **`haskell`** — Write and build Haskell (Cabal/Stack): purity, types, type classes, laziness. Use for .hs files and .cabal/stack.yaml. Detect ghc/cabal/stack first.
- **`hcl-lang`** — Write HCL for Terraform/Packer/Nomad: blocks, expressions, variables, functions. Use for .hcl/.tf files. Detect terraform/packer first.
- **`html`** — Write, analyze and validate semantic, accessible HTML. Use for .html files, templates, and markup structure/accessibility questions.
- **`java`** — Write, build and test Java (Maven/Gradle), Spring Boot or plain JVM. Use for .java files, pom.xml/build.gradle projects and Java build errors.
- **`javascript`** — Write, lint, format, test and debug modern JavaScript (ES2020+), Node and browser. Use for .js/.mjs/.cjs files and package.json projects.
- **`jsx-tsx`** — Work with JSX/TSX syntax used by React and similar libraries: components, props, hooks rules at the syntax level. Use for .jsx/.tsx files.
- **`julia`** — Write and run Julia for numerical/scientific computing with its package manager and test suite. Use for .jl files and Project.toml. Detect the julia binary first.
- **`kotlin`** — Write, build and test Kotlin (JVM/Android/Multiplatform): null-safety, coroutines, data classes. Use for .kt files and Gradle Kotlin projects.
- **`lisp-scheme`** — Write and run Common Lisp or Scheme (Racket/Guile): macros, recursion, REPL. Use for .lisp/.scm/.rkt files. Detect sbcl/racket/guile first.
- **`lua`** — Write and debug Lua for scripting and embedding (LOVE, Neovim, game hosts). Use for .lua files. Detect lua/luajit before executing.
- **`matlab`** — Read, write and reason about MATLAB/Octave numerical code. Use for .m files. Detect matlab or octave before claiming you can execute it.
- **`nim`** — Write and compile Nim, a statically typed language compiling to C/JS. Use for .nim files and .nimble projects. Detect nim before claiming support.
- **`objective-c`** — Read, write and maintain Objective-C with ARC and Cocoa patterns. Use for .m/.h Apple sources. Detect clang/xcodebuild first.
- **`ocaml`** — Write and build OCaml (Dune/opam): modules, variants, pattern matching. Use for .ml/.mli files and dune-project. Detect ocaml/dune first.
- **`odin`** — Write Odin, a systems language for data-oriented and game programming. Use for .odin files. Detect the odin binary first.
- **`perl`** — Read, write and modernize Perl 5 scripts with strict/warnings and testing. Use for .pl/.pm files. Detect perl before executing.
- **`php`** — Write, lint, analyze and test PHP (8+). Use for .php files, composer.json projects, Laravel/Symfony code.
- **`prolog`** — Write logic programs in Prolog: facts, rules, unification, backtracking, cut. Use for .pl Prolog files. Detect swipl first.
- **`protobuf-lang`** — Define Protocol Buffers schemas: messages, fields, evolution rules, codegen. Use for .proto files. Detect protoc/buf first.
- **`python`** — Write, lint, format, type-check, test and package Python 3. Use for .py files, pyproject.toml/requirements.txt projects, and Python errors.
- **`r-lang`** — Write and run R for statistics and data analysis, including tidyverse workflows. Use for .R/.Rmd files and RStudio projects. Detect Rscript before claiming support.
- **`raku`** — Write Raku (Perl 6): grammars, junctions, gradual typing, concurrency. Use for .raku/.rakumod files. Detect raku first.
- **`ruby`** — Write, lint, format and test Ruby (and Rails) code. Use for .rb files, Gemfile projects, and RSpec/Minitest suites.
- **`rust`** — Write, lint, format, test and build Rust: ownership, borrowing, error handling, traits. Use for .rs files, Cargo.toml projects, and borrow-checker errors.
- **`scala`** — Write and build Scala (sbt) with functional and typed patterns. Use for .scala files and build.sbt projects. Detect sbt/scala before claiming support.
- **`scss-sass`** — Author SCSS/Sass with variables, mixins, nesting and partials that compile to clean CSS. Use for .scss/.sass files.
- **`shell-bash`** — Write robust, portable Bash/POSIX shell scripts with strict mode and shellcheck. Use for .sh files and shell one-liners on POSIX systems.
- **`shell-powershell`** — Write PowerShell (5.1 and 7+) scripts and cmdlets with proper error handling and object pipelines. Use for .ps1/.psm1 files on Windows or cross-platform pwsh.
- **`solidity`** — Write and audit Solidity smart contracts with a strong security focus: reentrancy, overflow, access control, gas. Use for .sol files. Detect solc/foundry/hardhat first.
- **`swift`** — Write, build and test Swift (SwiftPM, iOS/macOS): optionals, value types, protocols, concurrency. Use for .swift files and Package.swift. Detect swift/xcodebuild first.
- **`tcl`** — Write Tcl scripts (and Tk/Expect): everything-is-a-string, lists, procs. Use for .tcl files. Detect tclsh first.
- **`typescript`** — Write and type-check TypeScript with strict types, generics and inference. Use for .ts files, tsconfig.json projects, and type errors.
- **`v-lang`** — Write V, a simple compiled language with fast builds and memory safety options. Use for .v files. Detect the v binary first.
- **`vala`** — Write Vala (compiles to C via GObject) for GNOME/GTK apps. Use for .vala files. Detect valac first.
- **`vbnet`** — Read and maintain VB.NET (.NET) code. Use for .vb files and legacy .NET projects. Detect dotnet before claiming support.
- **`verilog-vhdl`** — Write and simulate hardware description in Verilog/SystemVerilog/VHDL: modules, clocking, testbenches. Use for .v/.sv/.vhd files. Detect iverilog/ghdl first.
- **`webassembly`** — Build, inspect and debug WebAssembly modules and their JS/host bindings from Rust/C/AssemblyScript toolchains. Use for .wasm/.wat and wasm-pack projects.
- **`zig`** — Write and build Zig with explicit allocators, comptime and C interop. Use for .zig files and build.zig. Detect the zig binary before claiming support.

## Networking (4)

- **`api-gateway`** — Design API gateways: routing, auth, rate limiting, transformation, aggregation. Use when fronting multiple services/APIs.
- **`dns-management`** — Manage and debug DNS: record types, TTLs, propagation, resolvers, DNSSEC. Use for DNS setup/troubleshooting.
- **`http-protocol`** — Understand HTTP deeply: methods, status codes, headers, caching, cookies, HTTP/2-3, keep-alive. Use for HTTP-level debugging/design.
- **`networking-fundamentals`** — Understand and debug networking: OSI layers, TCP/UDP, IP, DNS, NAT, ports, connectivity troubleshooting. Use for network issues.

## Platforms (27)

- **`cloud-aws`** — Build on AWS: EC2, S3, Lambda, RDS, IAM, VPC, and the CLI. Use for AWS infrastructure/services. Detect the aws cli first.
- **`cloud-azure`** — Build on Azure: VMs, Blob Storage, Functions, AKS, Entra ID, and the CLI. Use for Azure services. Detect the az cli first.
- **`cloud-cloudflare`** — Build on Cloudflare: Workers, Pages, R2, KV, D1, CDN/WAF. Use for Cloudflare edge projects. Detect wrangler first.
- **`cloud-cost-optimization`** — Reduce cloud spend: right-sizing, autoscaling, reserved/spot, storage tiers, waste detection. Use when cloud costs are high.
- **`cloud-gcp`** — Build on Google Cloud: GCE, GCS, Cloud Functions/Run, GKE, IAM, and gcloud. Use for GCP services. Detect gcloud first.
- **`cloud-managed-kubernetes`** — Operate managed Kubernetes (EKS/AKS/GKE): node pools, autoscaling, IAM integration, upgrades. Use for managed k8s clusters.
- **`cloud-migration`** — Plan cloud migrations: assessment, 6 Rs, phased cutover, data migration, rollback. Use when moving workloads to/between clouds.
- **`cloud-networking`** — Design cloud networking: VPCs, subnets, security groups, peering, private endpoints, DNS. Use for cloud network architecture.
- **`cloud-serverless`** — Design serverless systems (Lambda/Functions/Workers): cold starts, statelessness, event-driven, limits. Use for FaaS architectures.
- **`cloud-storage`** — Use cloud object/block/file storage well: classes, lifecycle, encryption, access control, durability. Use for storage design.
- **`desktop-electron`** — Build cross-platform desktop apps with Electron: main/renderer, IPC, security (contextIsolation), packaging. Use for Electron projects.
- **`desktop-flutter`** — Build desktop apps with Flutter for Windows/macOS/Linux: platform channels, window management. Use for Flutter desktop targets.
- **`desktop-gtk`** — Build Linux desktop apps with GTK (C/Rust/Python/Vala): widgets, GObject, main loop. Use for GTK projects.
- **`desktop-javafx`** — Build cross-platform desktop apps with JavaFX (Java/Kotlin): FXML, scene graph, properties. Use for JavaFX projects.
- **`desktop-qt`** — Build native desktop apps with Qt (C++/QML) or PySide/PyQt: widgets, signals/slots, event loop. Use for Qt projects.
- **`desktop-tauri`** — Build lightweight desktop apps with Tauri (Rust + web frontend): commands, permissions, bundling. Use for Tauri projects. Detect tauri/cargo first.
- **`desktop-wpf`** — Build Windows desktop apps with WPF/WinUI (C#/XAML): MVVM, data binding, commands. Use for WPF/WinUI projects.
- **`mobile-android`** — Build native Android apps (Kotlin/Jetpack): lifecycle, coroutines, navigation, storage. Use for Android projects. Detect gradle first.
- **`mobile-app-release`** — Prepare and ship mobile releases: signing, store metadata, versioning, review guidelines, staged rollout. Use when publishing to App Store/Play.
- **`mobile-expo`** — Build and ship React Native apps with Expo: EAS build/update, config plugins, OTA updates. Use for Expo projects.
- **`mobile-flutter`** — Build cross-platform apps with Flutter/Dart: widgets, state management, navigation, platform channels. Use for Flutter projects. Detect flutter first.
- **`mobile-ios`** — Build native iOS apps (Swift/UIKit/SwiftUI): lifecycle, navigation, storage, App Store constraints. Use for iOS projects. Detect xcodebuild first.
- **`mobile-jetpack-compose`** — Build Android UI with Jetpack Compose: composables, state hoisting, recomposition, performance. Use for Compose code.
- **`mobile-kmp`** — Share code across platforms with Kotlin Multiplatform: expect/actual, shared modules, platform APIs. Use for KMP projects.
- **`mobile-maui`** — Build cross-platform apps with .NET MAUI: XAML, MVVM, platform handlers. Use for MAUI projects. Detect dotnet first.
- **`mobile-react-native`** — Build cross-platform apps with React Native/Expo: components, navigation, native modules, performance. Use for RN projects.
- **`mobile-swiftui`** — Build SwiftUI interfaces: declarative views, @State/@Observable, navigation, performance. Use for SwiftUI code.

## Quality (13)

- **`ab-testing`** — Run valid A/B experiments: hypotheses, sample size, significance, guardrail metrics, avoiding peeking. Use for data-driven product decisions.
- **`architecture-decision-records`** — Capture architectural decisions as ADRs: context, decision, consequences, status. Use when making significant technical decisions.
- **`code-comments`** — Write comments that explain why, not what; keep them accurate; avoid comment rot. Use when documenting non-obvious code.
- **`data-privacy`** — Handle personal data responsibly: minimization, consent, retention, anonymization, subject rights (GDPR/CCPA). Use when handling PII.
- **`error-handling`** — Handle errors robustly: fail loud, propagate context, no silent swallowing, typed errors, retries where safe. Use whenever code can fail.
- **`estimation-planning`** — Estimate and plan work realistically: decomposition, uncertainty, ranges not points, buffers, scope negotiation. Use when planning tasks.
- **`i18n-l10n`** — Internationalize and localize apps: message catalogs, pluralization, dates/numbers, RTL, encoding. Use for multi-language products.
- **`logging-best-practices`** — Log usefully: levels, structure, correlation ids, no secrets/PII, actionable messages. Use when adding logging.
- **`opensource-publishing`** — Publish open-source responsibly: license, README, contributing, secret scrubbing, CI, releases. Use when open-sourcing a project.
- **`requirements-analysis`** — Clarify requirements before building: user needs, acceptance criteria, edge cases, non-functional needs, ambiguity. Use before implementation.
- **`silent-failure-detection`** — Find and fix silent failures: swallowed errors, bad fallbacks, missing propagation, ignored return codes. Use when reviewing for hidden failures.
- **`tech-writing`** — Write clear technical content: structure for the reader, concrete examples, no jargon dumps, task-oriented. Use for docs, RFCs, guides, PRs.
- **`technical-debt-management`** — Identify, track and pay down technical debt deliberately: inventory, prioritization, incremental refactor, boy-scout rule. Use when managing debt.

## Security (26)

- **`security-api`** — Secure APIs: authn/authz per endpoint, rate limiting, input validation, object-level authorization, versioned deprecation. Use when securing APIs.
- **`security-audit`** — Run a defensive security review of a codebase: secrets, dependencies, injection, authz, misconfig, OWASP Top 10. Use when auditing code for vulnerabilities.
- **`security-cloud`** — Secure cloud accounts: least-privilege IAM, encryption, public-access prevention, config/CSPM checks. Use when reviewing cloud posture.
- **`security-code-review`** — Review code specifically for security defects: taint flow, authz gaps, unsafe deserialization, injection sinks. Use as a security-focused review pass.
- **`security-compliance`** — Map controls to compliance frameworks (SOC2/ISO27001/GDPR/HIPAA/PCI): evidence, data handling, audits. Use for compliance-driven work.
- **`security-container`** — Secure containers: non-root, minimal images, dropped capabilities, read-only FS, image scanning. Use when hardening container workloads.
- **`security-crypto`** — Use cryptography correctly: standard algorithms, key management, hashing vs encryption, no homegrown crypto. Use when handling crypto/hashing.
- **`security-csrf`** — Prevent cross-site request forgery: anti-CSRF tokens, SameSite cookies, verifying origin. Use for state-changing endpoints with cookie auth.
- **`security-dast`** — Run dynamic application security testing against a running app (authorized only): scanning, fuzzing endpoints. Use for authorized black-box testing.
- **`security-dependencies`** — Find and fix vulnerable dependencies: audit tools, transitive risk, safe upgrades, lockfiles. Use for dependency/CVE management.
- **`security-forensics`** — Perform incident forensics: preserve evidence, timeline, indicators of compromise, scope containment. Use during/after a suspected breach.
- **`security-headers`** — Set defensive HTTP security headers: CSP, HSTS, X-Content-Type-Options, frame options, referrer policy. Use to harden web responses.
- **`security-iam`** — Design least-privilege IAM: roles, policies, scoping, temporary credentials, privilege review. Use for access-control/cloud-IAM design.
- **`security-input-validation`** — Validate and sanitize all untrusted input: allowlists, schemas, encoding at the right boundary. Use when handling external input.
- **`security-logging-monitoring`** — Build security logging and detection: audit trails, alerting on suspicious activity, tamper-resistance, no sensitive data in logs. Use for detection/monitoring.
- **`security-mobile`** — Secure mobile apps: secure storage, cert pinning, no secrets in binaries, platform permissions. Use when securing iOS/Android apps.
- **`security-network`** — Harden network security: segmentation, firewalls, least-exposure, TLS everywhere, zero trust. Use when designing/reviewing network exposure.
- **`security-pentest-recon`** — Perform authorized reconnaissance and enumeration for pentests/CTFs: scoping, footprinting, service enumeration. Use only with explicit authorization.
- **`security-sast`** — Run static application security testing (Semgrep/CodeQL): rules, triage, false positives. Use to scan source for vulnerabilities.
- **`security-secrets`** — Detect and prevent leaked secrets: scanning, .gitignore, env handling, rotation after exposure. Use to find/remove credentials in code or history.
- **`security-sql-injection`** — Prevent SQL/NoSQL injection: parameterized queries, ORMs, no string concatenation, least-privilege DB users. Use when building queries.
- **`security-ssrf`** — Prevent server-side request forgery: URL allowlists, blocking internal ranges/metadata endpoints, no user-controlled fetch targets. Use for outbound-request features.
- **`security-supply-chain`** — Harden the software supply chain: pinned deps, SBOM, signing, provenance, CI trust boundaries. Use to reduce build/dependency attack surface.
- **`security-threat-modeling`** — Model threats (STRIDE/attack trees): assets, entry points, trust boundaries, mitigations. Use when designing security-sensitive features.
- **`security-web`** — Defend web apps against OWASP Top 10: injection, XSS, broken auth/access control, SSRF, misconfig. Use when securing web endpoints.
- **`security-xss`** — Prevent cross-site scripting: contextual output encoding, CSP, safe templating, avoiding innerHTML. Use when rendering user content.

## Testing (8)

- **`bdd-testing`** — Practice behavior-driven development: Given-When-Then scenarios, shared language, executable specs (Cucumber/Gherkin). Use for behavior-focused specs.
- **`contract-testing`** — Verify service contracts (Pact/consumer-driven): prevent breaking API changes between producer and consumer. Use for service integrations.
- **`e2e-testing`** — Write end-to-end tests (Playwright/Cypress/Selenium): critical user journeys, stability, flakiness control. Use for full-stack UI flows.
- **`fuzzing`** — Fuzz test to find crashes/security bugs (libFuzzer/AFL/go-fuzz/Atheris): coverage-guided random inputs. Use for parsers/untrusted input.
- **`load-testing`** — Load/stress test systems (k6/Locust/JMeter/wrk): throughput, latency percentiles, breaking point. Use to validate performance/capacity.
- **`property-testing`** — Write property-based tests (Hypothesis/fast-check/QuickCheck): invariants over generated inputs, shrinking. Use for logic with many edge cases.
- **`snapshot-testing`** — Use snapshot tests wisely: UI/output regression, reviewing diffs, avoiding rubber-stamping. Use for output-shape regression.
- **`tdd-workflow`** — Practice test-driven development: red-green-refactor, small steps, test-first, behavior focus. Use when TDD is desired.

## Tooling (30)

- **`api-openapi`** — Design and use OpenAPI/Swagger specs: schema-first, validation, client/server codegen, docs. Use for REST API contracts.
- **`api-testing-tools`** — Test and explore APIs with curl/httpie/Postman/Bruno/Hurl: requests, collections, assertions, CI. Use for manual/automated API testing.
- **`ast-transformation`** — Transform code programmatically with ASTs/codemods (jscodeshift, ts-morph, Babel, libcst, comby). Use for large mechanical refactors.
- **`bazel-build-tool`** — Build large/polyglot monorepos with Bazel: BUILD files, hermeticity, remote cache. Use for Bazel projects. Detect bazel first.
- **`build-tool-make`** — Write correct Makefiles: targets, prerequisites, phony targets, variables, parallelism. Use for Makefile projects. Detect make first.
- **`changelog-release`** — Automate changelogs and releases (Conventional Commits, changesets, semantic-release): notes, tags, artifacts. Use when cutting releases.
- **`cli-design`** — Design good command-line interfaces: argument parsing, help, exit codes, stdout/stderr, machine-readable output. Use when building CLIs.
- **`cmake-build-tool`** — Configure C/C++ builds with CMake: targets, properties, find_package, presets, cross-compilation. Use for CMakeLists.txt. Detect cmake first.
- **`code-coverage`** — Measure and use code coverage meaningfully: line/branch coverage, gaps, avoiding coverage theater. Use when assessing test completeness.
- **`code-search`** — Search and navigate code efficiently: ripgrep, ast-grep, git grep, structural search, symbol lookup. Use to find code across a repo.
- **`dependency-management`** — Manage dependencies over time: upgrades, pinning, deduplication, minimizing footprint, renovate/dependabot. Use for keeping deps healthy.
- **`documentation-tools`** — Generate docs from code (Sphinx, JSDoc/TypeDoc, Doxygen, rustdoc, Docusaurus, MkDocs). Use to produce/maintain API docs.
- **`editorconfig`** — Standardize editor settings across a team with .editorconfig: indentation, line endings, charset. Use to prevent whitespace churn.
- **`env-config-management`** — Manage configuration and env vars across environments: 12-factor, .env, validation at boot, no secrets in code. Use for app configuration.
- **`git-advanced`** — Use advanced git: interactive rebase, cherry-pick, bisect, reflog, worktrees, submodules, recovery. Use for complex git operations.
- **`git-history-rewrite`** — Safely rewrite git history: remove secrets/large files (filter-repo/BFG), squash, reorder. Use to purge sensitive data or clean history. Destructive - confirm first.
- **`git-workflow-branching`** — Choose and run a branching strategy (trunk-based, GitFlow, GitHub flow) and clean PR hygiene. Use when setting team git workflow.
- **`gradle-build-tool`** — Configure Gradle builds (Groovy/Kotlin DSL): tasks, dependencies, caching, multi-project. Use for build.gradle(.kts). Detect gradle first.
- **`license-compliance`** — Manage open-source license compliance: SPDX, compatibility, attribution, copyleft awareness. Use before shipping/redistributing code.
- **`linting-formatting`** — Set up linters and formatters (ESLint/Prettier/ruff/black/gofmt/clippy) and enforce them in CI. Use to standardize code style/quality.
- **`maven-build-tool`** — Configure Maven builds: POM, lifecycle, plugins, dependency management, profiles. Use for pom.xml. Detect mvn first.
- **`monorepo-tools`** — Manage monorepos (Nx/Turborepo/pnpm workspaces/Lerna): task graphs, affected builds, caching, boundaries. Use for monorepo setups.
- **`mutation-testing`** — Assess test quality with mutation testing (Stryker/mutmut/PIT): surviving mutants reveal weak tests. Use to validate test effectiveness.
- **`package-managers`** — Use package managers correctly (npm/pnpm/yarn/pip/poetry/cargo/go/maven): lockfiles, reproducible installs, audits. Use for dependency install/management.
- **`pre-commit-hooks`** — Automate checks before commit/push with pre-commit/husky/lefthook: lint, format, secret scan, tests. Use to catch issues early.
- **`refactor-dead-code`** — Find and safely remove dead code, unused deps and duplication (knip, ts-prune, depcheck, vulture). Use to reduce cruft.
- **`regex-mastery`** — Write, debug and reason about regular expressions safely: anchors, groups, lookarounds, catastrophic backtracking. Use for pattern matching.
- **`static-analysis`** — Run static analysis/type checks/linters (mypy, tsc, clippy, staticcheck, SpotBugs) to catch bugs pre-runtime. Use to find defects without running code.
- **`tui-development`** — Build terminal UIs (Textual, Bubble Tea, Ink, ratatui, blessed): layout, input, rendering, resize. Use for interactive terminal apps.
- **`versioning-semver`** — Version software with SemVer: major/minor/patch, breaking-change discipline, pre-releases, deprecation. Use when releasing versions.

## Web (40)

- **`backend-auth`** — Implement authentication/authorization: sessions vs JWT, OAuth/OIDC, password hashing, RBAC. Use for login/auth features.
- **`backend-caching`** — Add caching (in-memory, Redis, HTTP) with correct invalidation and TTLs. Use when reducing latency/load.
- **`backend-django`** — Build Django apps: ORM, migrations, views, DRF, admin, security. Use for manage.py projects.
- **`backend-dotnet`** — Build ASP.NET Core services: minimal APIs/controllers, DI, EF Core, middleware. Use for .NET web projects.
- **`backend-express`** — Build Express APIs: routing, middleware, error handling, validation. Use for Express projects.
- **`backend-fastapi`** — Build FastAPI services: async endpoints, Pydantic models, dependency injection, OpenAPI. Use for FastAPI projects.
- **`backend-flask`** — Build Flask apps: blueprints, extensions, request handling, config. Use for Flask projects.
- **`backend-go-http`** — Build Go HTTP services with net/http or chi/gin: handlers, middleware, context. Use for Go web services.
- **`backend-graphql`** — Design and build GraphQL APIs: schema, resolvers, N+1 via dataloader, auth. Use for GraphQL servers/schemas.
- **`backend-grpc`** — Build gRPC services with protobuf: service definitions, streaming, deadlines, errors. Use for .proto files and gRPC services.
- **`backend-laravel`** — Build Laravel apps: Eloquent, migrations, routing, middleware, queues. Use for artisan projects.
- **`backend-nestjs`** — Build NestJS apps: modules, providers, DI, guards, pipes. Use for NestJS projects.
- **`backend-node`** — Build Node.js backends: async IO, streams, process/cluster, env config. Use for Node server projects.
- **`backend-phoenix`** — Build Phoenix (Elixir) web apps and LiveView. Use for Phoenix projects.
- **`backend-queues`** — Use message queues/background jobs (Redis, RabbitMQ, SQS, Celery, BullMQ): idempotency, retries, DLQ. Use for async processing.
- **`backend-rails`** — Build Ruby on Rails apps: ActiveRecord, migrations, controllers, jobs. Use for Rails projects.
- **`backend-rate-limiting`** — Protect APIs with rate limiting and throttling (token bucket, sliding window). Use to prevent abuse/overload.
- **`backend-rest-api`** — Design RESTful APIs: resources, status codes, versioning, pagination, idempotency, errors. Use when designing/reviewing HTTP APIs.
- **`backend-spring`** — Build Spring Boot services: controllers, services, JPA, security, config. Use for Spring Boot projects.
- **`backend-websockets`** — Build realtime features with WebSockets/SSE: connection lifecycle, backpressure, auth, scaling. Use for realtime servers.
- **`web-accessibility`** — Make web UI accessible to WCAG 2.2 AA: semantics, keyboard, focus, contrast, ARIA, screen readers. Use when building or auditing UI.
- **`web-component-testing`** — Test UI components by behavior with Testing Library / Playwright component tests. Use when adding frontend tests.
- **`web-forms-validation`** — Build accessible forms with client and server validation and good error UX. Use when implementing forms and input validation.
- **`web-frontend-angular`** — Build Angular apps: components, services, DI, RxJS, signals. Use for angular.json projects and .component.ts files.
- **`web-frontend-astro`** — Build Astro sites with islands architecture and content collections. Use for astro.config.mjs projects and .astro files.
- **`web-frontend-nextjs`** — Build Next.js apps: App Router, server/client components, routing, data fetching, SSR/SSG. Use for next.config.js projects.
- **`web-frontend-nuxt`** — Build Nuxt 3 apps: pages, composables, server routes, SSR. Use for nuxt.config.ts projects.
- **`web-frontend-qwik`** — Build Qwik apps with resumability and lazy execution. Use for Qwik projects. Detect the toolchain first.
- **`web-frontend-react`** — Build React apps: components, hooks, state, effects, performance. Use for React projects (react in package.json), .jsx/.tsx components and hook bugs.
- **`web-frontend-remix`** — Build Remix apps: loaders, actions, nested routes, progressive enhancement. Use for Remix projects.
- **`web-frontend-solid`** — Build SolidJS apps with fine-grained reactivity and signals. Use for Solid projects and .tsx Solid components.
- **`web-frontend-svelte`** — Build Svelte/SvelteKit apps with reactive statements and stores. Use for .svelte files and SvelteKit projects.
- **`web-frontend-vue`** — Build Vue 3 apps: Composition API, reactivity, SFCs, Pinia. Use for .vue files and Vue projects.
- **`web-performance`** — Optimize web performance: Core Web Vitals, bundle size, lazy loading, caching, rendering. Use when pages are slow or fail CWV.
- **`web-pwa`** — Add PWA features: service workers, offline caching, manifest, installability. Use when making a web app installable/offline.
- **`web-seo`** — Optimize technical SEO: metadata, structured data, sitemaps, canonical, crawlability, performance. Use for public sites.
- **`web-state-management`** — Choose and apply frontend state management (local, context, Redux, Zustand, Pinia, signals) appropriately. Use when state grows tangled.
- **`web-tailwind`** — Style with Tailwind CSS utility classes and design tokens. Use for tailwind.config projects and utility-class styling.
- **`web-vite`** — Configure and debug Vite builds, dev server, plugins and env handling. Use for vite.config projects.
- **`web-webpack`** — Configure and debug webpack: loaders, plugins, code splitting, tree shaking. Use for webpack.config.js projects.

## Web3 (6)

- **`blockchain-indexing`** — Index on-chain data (The Graph/subgraphs/custom indexers): events, reorg handling, queries. Use for reading chain data at scale.
- **`defi-patterns`** — Reason about DeFi mechanisms and their risks: AMMs, lending, oracles, flash loans, MEV. Use for DeFi contract/design review. Defensive/educational only.
- **`nft-metadata`** — Design NFT metadata and storage: ERC-721/1155, tokenURI, IPFS/Arweave, immutability, standards. Use for NFT contracts/collections.
- **`smart-contract-testing`** — Test smart contracts (Foundry/Hardhat): unit, fuzz, invariant, fork tests, gas snapshots. Use for contract test suites.
- **`wallet-integration`** — Integrate wallets (MetaMask/WalletConnect/RainbowKit): connect flow, chain switching, signing, UX. Use for dApp wallet flows.
- **`web3-ethers`** — Interact with EVM chains from JS/TS (ethers.js/viem/web3.js): providers, signers, contracts, events, gas. Use for dApp frontends/backends.

