---
name: backstage-idp
description: "Build a developer portal with Backstage: software catalog, templates, TechDocs, plugins. Use for Backstage/IDP projects."
---

# Backstage Idp

**Category:** devops  ·  **Requires (detect first):** yarn

## Required tools & detection

Before acting, confirm these are installed (see the `environment-detection` skill). If a tool is missing, say so and suggest how to install it — never pretend it is present.

- `yarn`

## Key commands

```bash
yarn dev
yarn build
```

## Common pitfalls

- catalog-info drift/rot
- Heavy plugin maintenance burden
- No ownership metadata

## Related skills

- `platform-engineering`
- `documentation-tools`
- `api-openapi`

---

_Safety: destructive actions require explicit confirmation; secrets are redacted before output. See the `safe-execution` skill._
