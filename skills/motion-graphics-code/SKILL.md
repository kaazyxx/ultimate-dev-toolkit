---
name: motion-graphics-code
description: "Create motion graphics in code (Remotion/Lottie/GSAP/CSS): keyframes, easing, timelines, render pipelines. Use for programmatic animation/video."
---

# Motion Graphics Code

**Category:** creative  ·  **Requires (detect first):** npm

## Required tools & detection

Before acting, confirm these are installed (see the `environment-detection` skill). If a tool is missing, say so and suggest how to install it — never pretend it is present.

- `npm`

## Key commands

```bash
npx remotion render
npm run build
```

## Common pitfalls

- Janky animation (layout thrash)
- Non-deterministic renders
- Huge render times unoptimized

## Related skills

- `make-interfaces-feel-better`
- `video-processing`
- `creative-coding`

---

_Safety: destructive actions require explicit confirmation; secrets are redacted before output. See the `safe-execution` skill._
