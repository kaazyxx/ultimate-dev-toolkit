---
name: module-federation
description: "Share code at runtime with Module Federation: remotes, shared singletons, versioning, fallback. Use for micro-frontends/plugin UIs."
---

# Module Federation

**Category:** frontend-adv

## Common pitfalls

- Singleton version mismatches
- Remote load failures with no fallback
- Over-sharing bloating the host

## Related skills

- `micro-frontends`
- `web-webpack`
- `web-vite`

---

_Safety: destructive actions require explicit confirmation; secrets are redacted before output. See the `safe-execution` skill._
