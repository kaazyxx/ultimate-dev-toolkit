---
name: janet
description: "Write Janet: a Lisp-like functional/imperative language for scripting and embedding. Use for .janet files. Detect janet first."
---

# Janet

**Category:** languages  ·  **Requires (detect first):** janet

## Required tools & detection

Before acting, confirm these are installed (see the `environment-detection` skill). If a tool is missing, say so and suggest how to install it — never pretend it is present.

- `janet`

## Key commands

```bash
janet main.janet
```

## Common pitfalls

- Macro hygiene mistakes
- Fiber/coroutine misuse

## Related skills

- `lisp-scheme`
- `clojure`
- `lua`

---

_Safety: destructive actions require explicit confirmation; secrets are redacted before output. See the `safe-execution` skill._
