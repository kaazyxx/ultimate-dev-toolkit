---
name: api-fuzzing
description: "Fuzz-test APIs (RESTler/schemathesis) against the schema: malformed inputs, edge cases, security. Use to find robustness/security bugs in APIs."
---

# Api Fuzzing

**Category:** qa

## Common pitfalls

- Only testing the happy path
- No schema to fuzz against
- Ignoring auth/stateful sequences

## Related skills

- `fuzzing`
- `api-openapi`
- `security-api`

---

_Safety: destructive actions require explicit confirmation; secrets are redacted before output. See the `safe-execution` skill._
