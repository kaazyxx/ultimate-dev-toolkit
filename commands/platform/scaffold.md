---
description: Scaffold a new service from a golden-path template.
argument-hint: "<service-name>"
allowed-tools: Read, Write, Edit, Bash
---

Scaffold a new service named per $ARGUMENTS using the `platform-engineering`/`project-scaffolding` skills. Follow the org's golden path if one exists (templates, CI, ownership metadata, observability). Otherwise set up a conventional, tested, CI-wired service. Report what was created.
