---
description: Run tests in watch mode for a fast feedback loop.
argument-hint: "[path]"
allowed-tools: Bash
---

Start the test runner in watch mode for $ARGUMENTS if the framework supports it. Explain how to interpret the loop and stop it. Use this for TDD-style fast feedback.
