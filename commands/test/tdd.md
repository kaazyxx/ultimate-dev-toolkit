---
description: Drive a feature with red-green-refactor TDD.
argument-hint: "<feature>"
allowed-tools: Read, Write, Edit, Bash
---

Implement the feature in $ARGUMENTS using the `tdd-workflow` skill. Write a failing test first (red), the minimal code to pass (green), then refactor with tests green. Work in small steps and show the loop. Do not write the implementation before the test.
