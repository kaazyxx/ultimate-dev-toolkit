---
description: Add property-based tests for logic with many edge cases.
argument-hint: "<file-or-function>"
allowed-tools: Read, Write, Edit, Bash
---

Add property-based tests for $ARGUMENTS using the `property-testing` skill (Hypothesis/fast-check/QuickCheck). Identify invariants, write generators covering the input space, and run them (with shrinking). Verify they actually exercise edge cases.
