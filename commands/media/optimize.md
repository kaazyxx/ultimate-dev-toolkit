---
description: Optimize images/media assets for the web (size/format).
argument-hint: "[path]"
allowed-tools: Bash, Read
---

Optimize the assets in $ARGUMENTS using the `image-processing`/`digital-asset-management`/`web-performance` skills. Generate responsive sizes and modern formats (WebP/AVIF), strip sensitive metadata, and report before/after sizes. Do not overwrite originals without confirmation.
