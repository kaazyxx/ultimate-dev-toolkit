---
description: Explain what a SQL query does and its performance characteristics.
argument-hint: "<sql>"
allowed-tools: Read
---

Explain the SQL in $ARGUMENTS in plain language: what it returns, the joins/filters, and likely performance characteristics (scans, sorts, cardinality). Point out correctness risks (cartesian joins, NULL logic, missing WHERE).
