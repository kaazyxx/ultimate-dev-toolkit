---
description: Summarize recent CI run results and failures.
allowed-tools: Bash
---

Summarize recent CI status using `gh run list`/`glab ci status` if available. Report pass/fail of recent runs and, for failures, the failing job and likely cause from its log. Offer to help fix a specific failure.
