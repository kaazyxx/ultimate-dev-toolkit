---
description: Show the active gcloud account and project.
allowed-tools: Bash
---

Show the active GCP context using the `cloud-gcp` skill. Detect gcloud, run `gcloud auth list` and `gcloud config list`, and report account/project. Read-only.
