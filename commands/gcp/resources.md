---
description: List key GCP resources for the current project (read-only).
argument-hint: "[project]"
allowed-tools: Bash
---

List GCP resources for $ARGUMENTS using the `cloud-gcp`/`security-cloud` skills. Summarize compute/storage and flag public buckets and broad IAM roles. Read-only; never create/modify/delete.
