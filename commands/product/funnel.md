---
description: Analyze a conversion funnel and find the biggest drop-off.
argument-hint: "<funnel-steps-or-data>"
allowed-tools: Read, Bash
---

Analyze the funnel in $ARGUMENTS using the `conversion-optimization`/`product-analytics` skills. Identify the biggest drop-off step, form hypotheses about the friction, and propose prioritized experiments (with success metrics and guardrails). Base it on data, not guesses.
