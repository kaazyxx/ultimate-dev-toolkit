---
description: Draft a tracking plan (events, properties, funnels) for a feature.
argument-hint: "<feature>"
allowed-tools: Read, Write
---

Draft an analytics tracking plan for $ARGUMENTS using the `analytics-instrumentation`/`product-analytics` skills. Define events with consistent naming, typed properties, funnels and success metrics — and explicitly keep PII out of properties. Output a table the team can implement against.
