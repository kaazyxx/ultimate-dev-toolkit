---
description: Show the status/logs of the most recent pipeline run.
allowed-tools: Read
---

Report the status of the last pipeline run using the `pipelines` skill: which steps passed/failed, timings, and the failing step's log if any. Read from the pipeline run records under `.udt/` if present.
