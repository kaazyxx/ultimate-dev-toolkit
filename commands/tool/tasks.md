---
description: Discover and run project tasks (just/make/npm scripts).
argument-hint: "[task]"
allowed-tools: Read, Bash
---

Discover the project's task runner (justfile / Makefile / package.json scripts / Taskfile) using the `just-task-runner`/`build-tool-make` skills. List available tasks and, if one is named in $ARGUMENTS, run it and report the result.
