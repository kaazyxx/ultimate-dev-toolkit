---
description: "Bootstrap a new module/component with the project's conventions."
argument-hint: "<type> <name>"
allowed-tools: Read, Glob, Write, Edit, Bash
---

Scaffold what $ARGUMENTS describes using the `project-scaffolding` and `code-generation` skills.

Learn the existing structure/conventions first, then create the files (module/component/endpoint/test) consistent with them, wire them in, and add a minimal test. Use the ecosystem's official generator when one fits. Report what was created.
