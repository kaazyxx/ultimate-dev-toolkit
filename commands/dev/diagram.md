---
description: Generate a Mermaid diagram of the code/architecture.
argument-hint: "[path]"
allowed-tools: Read, Grep, Glob
---

Produce a Mermaid diagram of $ARGUMENTS using the `code-analysis`/`architecture-patterns` skills. Map modules, layers and key data flow from the real code, then output a valid ```mermaid``` flowchart (renders on GitHub). Keep it readable, not exhaustive.
