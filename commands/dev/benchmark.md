---
description: Measure performance of a code path and report results.
argument-hint: "<target>"
allowed-tools: Read, Bash
---

Benchmark the target in $ARGUMENTS using the `performance-profiling` guidance.

Create or run a repeatable benchmark, capture real numbers (multiple runs), and report throughput/latency with variance. Do not present a single noisy run as fact. If no benchmark harness exists, propose one.
