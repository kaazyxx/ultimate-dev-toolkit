---
description: Set up a reproducible dev environment (nix/direnv/devcontainer).
argument-hint: "[nix|direnv|devcontainer]"
allowed-tools: Read, Write, Bash
---

Set up a reproducible dev environment for this project per $ARGUMENTS using the `nix-package-manager`/`direnv-envrc`/`env-config-management` skills. Detect the stack, generate the config (flake.nix / .envrc / devcontainer.json), pin versions, and keep secrets out. Explain how to activate it.
