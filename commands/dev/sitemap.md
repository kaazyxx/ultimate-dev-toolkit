---
description: Generate a sitemap/site structure for a static site.
argument-hint: "[path]"
allowed-tools: Read, Glob, Write
---

Generate a sitemap and structure overview for $ARGUMENTS using the `static-site-generators`/`web-seo` skills. Enumerate routes/content, produce sitemap.xml, and flag orphan pages or broken internal links.
