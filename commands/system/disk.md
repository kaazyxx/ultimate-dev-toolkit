---
description: Report disk usage and find large files/directories.
argument-hint: "[path]"
allowed-tools: Bash
---

Report disk usage for $ARGUMENTS using the `system-monitoring` skill. Show free/used per volume and the largest directories/files. Suggest safe cleanup candidates but never delete without confirmation.
