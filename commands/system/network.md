---
description: Diagnose local connectivity, DNS and open ports.
argument-hint: "[host-or-port]"
allowed-tools: Bash
---

Diagnose networking for $ARGUMENTS using the `networking-fundamentals` skill. Check connectivity (ping/traceroute), DNS (dig/nslookup), and listening ports (ss/netstat). Isolate whether an issue is DNS, connectivity or a firewall/port problem.
