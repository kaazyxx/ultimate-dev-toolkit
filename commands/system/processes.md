---
description: Show top resource-consuming processes.
argument-hint: "[cpu|mem]"
allowed-tools: Bash
---

Show the top processes by CPU or memory (per $ARGUMENTS) using the `system-monitoring` skill (Get-Process on Windows, top/ps on POSIX). Identify likely resource hogs and note if anything looks abnormal.
