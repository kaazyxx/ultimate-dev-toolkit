---
description: Plan a micro-frontend split with shared-dependency strategy.
argument-hint: "<app-description>"
allowed-tools: Read, Grep, Glob
---

Plan a micro-frontend architecture for $ARGUMENTS using the `micro-frontends`/`module-federation` skills. Define boundaries, shared singleton deps and versions, runtime composition, and failure fallbacks. Flag where a monolith is the better call.
