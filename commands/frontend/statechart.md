---
description: Model a complex UI flow as an explicit state machine.
argument-hint: "<flow-description>"
allowed-tools: Read, Write, Grep, Glob
---

Model $ARGUMENTS as a statechart using the `state-machines` skill (XState). Enumerate states, transitions, guards and side effects, eliminate impossible states, and output the machine definition plus a diagram description.
