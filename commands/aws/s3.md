---
description: List/inspect S3 buckets and flag public exposure (read-only).
argument-hint: "[bucket]"
allowed-tools: Bash
---

Inspect S3 for $ARGUMENTS using the `cloud-aws`/`security-cloud` skills. List buckets (or a bucket's objects), and check public-access-block and bucket policy for any public exposure. Report findings; never upload, delete or make anything public.
