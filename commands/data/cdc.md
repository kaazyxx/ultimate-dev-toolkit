---
description: Design or review a change-data-capture pipeline.
argument-hint: "[source]"
allowed-tools: Read, Bash
---

Design or review a CDC pipeline for $ARGUMENTS using the `change-data-capture` skill. Ensure log-based capture, ordering, idempotent consumers, schema-change handling and backpressure protection on the source. Report the design and risks.
