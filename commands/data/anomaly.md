---
description: Detect anomalies/outliers in a dataset or metric.
argument-hint: "<file-or-series>"
allowed-tools: Read, Bash
---

Detect anomalies in $ARGUMENTS using the `anomaly-detection` skill. Account for seasonality/trend, choose an appropriate method, and report flagged points with context and confidence. Avoid false positives from static thresholds.
