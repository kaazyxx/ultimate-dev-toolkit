---
description: Set up or check dataset/pipeline versioning (DVC).
argument-hint: "[path]"
allowed-tools: Read, Bash
---

Set up or verify data versioning for $ARGUMENTS using the `data-versioning` skill (DVC). Ensure large files are tracked by DVC not git, pipelines are reproducible (dvc repro), and lineage is intact. Report the state and next steps.
