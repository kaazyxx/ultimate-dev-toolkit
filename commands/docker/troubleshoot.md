---
description: Diagnose a failing/unhealthy container or build.
argument-hint: "<container-or-image>"
allowed-tools: Bash
---

Troubleshoot the Docker problem in $ARGUMENTS using the `docker-ops` and `debugging` skills. Check logs, inspect config, exit codes and healthchecks. Identify the root cause (bad entrypoint, missing env, port conflict, OOM) and propose a fix.
