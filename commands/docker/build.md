---
description: Build an image and review the Dockerfile for size/security.
argument-hint: "[path] [tag]"
allowed-tools: Bash, Read
---

Build the image for $ARGUMENTS using the `docker-fundamentals`/`docker-image-optimization` skills. Review the Dockerfile for multi-stage, non-root user, layer caching and secret leakage before/while building. Report image size and improvement suggestions.
