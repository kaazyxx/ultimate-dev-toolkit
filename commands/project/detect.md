---
description: Detect language, framework, package manager and build system.
argument-hint: "[path]"
allowed-tools: Read, Glob, Bash
---

Detect the project stack for $ARGUMENTS using the `environment-detection` skill, from real manifest/lockfiles and config. Report language, framework, package manager, build system and test framework, plus which UDT skills apply. State detected vs. assumed.
