---
description: Produce a newcomer onboarding overview of the project.
argument-hint: "[path]"
allowed-tools: Read, Glob, Grep, Bash
---

Onboard a newcomer to $ARGUMENTS using the `code-analysis` skill. Explain what the project does, its architecture and entry points, how to build/run/test it, and where the important code lives. Point out gotchas. Base everything on the real code, not guesses.
