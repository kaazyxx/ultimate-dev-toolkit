---
description: List Azure resource groups and key resources (read-only).
argument-hint: "[resource-group]"
allowed-tools: Bash
---

List Azure resources for $ARGUMENTS using the `cloud-azure` skill. Summarize resource groups and notable resources, flagging untagged sprawl and public exposure. Read-only; never create/modify/delete.
