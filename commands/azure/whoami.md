---
description: Show the current Azure account and subscription.
allowed-tools: Bash
---

Show the active Azure context using the `cloud-azure` skill. Detect the az CLI, run `az account show`, and report subscription and tenant (redacting ids as needed). Read-only.
