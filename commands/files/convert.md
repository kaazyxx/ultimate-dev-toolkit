---
description: Convert a file between formats losslessly.
argument-hint: "<file> --to <format>"
allowed-tools: Read, Bash, Write
---

Convert the file in $ARGUMENTS using the `data-format-conversion` skill. Pick the right tool for the format pair, preserve data/types and encoding, and verify the round-trip. Confirm before overwriting an existing target.
