---
description: Show an annotated project structure overview.
argument-hint: "[path] [depth]"
allowed-tools: Bash, Glob
---

Show the directory structure of $ARGUMENTS (sensible default depth), ignoring node_modules/.git/build artifacts. Annotate the important directories and entry points so a newcomer can orient quickly.
