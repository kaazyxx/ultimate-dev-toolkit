---
description: Design a clean, consistent, evolvable API for a resource.
argument-hint: "<resource-or-feature>"
allowed-tools: Read, Write
---

Design the API for $ARGUMENTS using the `api-design` skill. Define resources, methods, status codes, error shape, pagination and versioning, and note backward-compatibility. Produce a concise spec (and an OpenAPI stub if REST). Keep conventions consistent with any existing API.
