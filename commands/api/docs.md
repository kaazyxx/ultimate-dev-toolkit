---
description: Generate or update API documentation from the spec/code.
argument-hint: "[spec-or-path]"
allowed-tools: Read, Write, Bash
---

Produce API docs for $ARGUMENTS using the `api-openapi` and `documentation-writer` skills. Derive them from the real spec/routes (source of truth), include request/response examples and error responses, and keep them in sync with the implementation.
