---
description: Set up or check consumer-driven contract tests between services.
argument-hint: "[consumer] [provider]"
allowed-tools: Read, Bash, Write
---

Work on contract tests for $ARGUMENTS using the `contract-testing` skill. Capture consumer expectations as contracts and verify the provider against them so breaking changes are caught in CI, not production. Keep contracts about behavior, not internals.
