---
description: Run visual regression tests and review diffs.
argument-hint: "[path]"
allowed-tools: Read, Bash
---

Run visual regression for $ARGUMENTS using the `snapshot-testing`/`e2e-testing` skills (Playwright/Percy/Chromatic if present). Capture and compare screenshots, review real differences vs noise, and report which changes are intended vs regressions.
