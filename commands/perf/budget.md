---
description: Define and check performance budgets (bundle/CWV).
argument-hint: "[path]"
allowed-tools: Read, Bash
---

Define and check performance budgets for $ARGUMENTS using the `web-performance` skill. Set budgets for bundle size and Core Web Vitals, measure current values (lighthouse/bundle analyzer), and report which budgets pass/fail with the top offenders.
