---
description: Define a governed metrics/semantic layer.
argument-hint: "[path]"
allowed-tools: Read, Grep, Glob
---

Design or review a semantic layer for $ARGUMENTS using the `semantic-layer` skill. Centralize metric definitions, assign ownership/governance, and prevent divergent metrics across BI tools. Report the model and gaps. Read-only.
